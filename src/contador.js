

function es_igual(palabra) {
    contador = 0
    if (palabra === palabra)
        contador = contador +1

    return contardor;
  }
  
 // export default contador;


function contarPalabra(frase){
    length = frase;//cantidad de palabras
    vecFrase[0]=frase//meter la frase en un vector

    for(i=0;i<length;i++){

        vecFrase[i]=frase

    }

    for(i=0;i<length;i++){

        es_igual(i);//verificar palabras repetidas y contarlas en otro vector

    }

}
export default contarPalabra;